// The Array.mpa function method returns a new array
// with the result of calling a provided function
// on every element in this array

const list = [1, 2, 3, 4, 5];
console.log(list.map((element) => element * 2)); // Output: [2, 4, 6, 8, 10]
