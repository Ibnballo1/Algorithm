// Array.filter returns a new array with
// all elements that pass the test implemented
// provided function

const list = [1, 2, 3, 4, 5, 6];
console.log(list.filter((element) => element % 2 === 0)); // Output: [2, 4, 6]
